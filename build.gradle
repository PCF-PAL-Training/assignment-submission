buildscript {
    repositories {
        maven { 
            url "http://platform-acceleration-lab-maven.s3.amazonaws.com" 
        }
        jcenter()
    }

    dependencies {
        classpath "io.pivotal.pal.assignments:assignments-plugin:3.5.1"
    }
}

apply plugin: "io.pivotal.pal.assignments.plugin"

assignments {
    apiUrl = "https://assignments.education.pivotal.io/"
    emails = [
       "madanraj.venkatesan@gmail.com"
    ]
    cohortIdentifier = "349802431"
}
defaultTasks "tasks"